collectors:

- type: js-npm
  path: /
  actors:
  - type: js-npm
    versions: "Y.Y.Y"
    settings:
      batch_mode: true
      github_labels:
      - dependencies:update
      github_assignees:
      - isoppp
