collectors:

- type: js
  path: /
  actors:
  # pull requests for all major, minor, and patch updates
  - type: js-npm
    versions: "Y.Y.Y"
    settings:
      github_labels:
      - dependencies
      github_assignees:
      - isoppp
      batch_mode: true


